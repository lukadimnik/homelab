apiVersion: apps/v1
kind: Deployment
metadata:
  name: audiobookshelf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audiobookshelf
  template:
    metadata:
      labels:
        app: audiobookshelf
    spec:
      securityContext:
        fsGroup: 1000 # ownership of the volume
        runAsUser: 1000 # user node user ID
        runAsGroup: 1000 # user node group ID
      containers:
        - name: audiobookshelf
          envFrom:
            - configMapRef:
                name: audiobookshelf-configmap 
          image: ghcr.io/advplyr/audiobookshelf:2.35.1
          ports:
            - containerPort: 3005
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: audiobookshelf-data
              mountPath: /config
            - name: audiobookshelf-data
              mountPath: /metadata
            - name: audiobookshelf-data
              mountPath: /audiobooks
            - name: audiobookshelf-data
              mountPath: /podcasts
      restartPolicy: Always
      volumes:
        - name: audiobookshelf-data
          persistentVolumeClaim:
            claimName: audiobookshelf-data-pvc
